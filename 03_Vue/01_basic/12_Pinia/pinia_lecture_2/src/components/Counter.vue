<template>
  <h3>반응형으로 꺼내기 : {{ counter.count }}</h3>
  <h3>값만 꺼내기 : {{ count }}</h3>
  <button @click="increment">+ 증가</button>
  <button @click="counter.reset">초기화</button>
</template>

<script setup>
import { useCounterPersistStore } from '@/stores/persist/persist_counter';
//import { useCounterStore } from '@/stores/counter';

// State를 꺼낼때는 구조분해 할당을 하게 되면, 반응형이 깨짐
//const counter = useCounterStore(); // 객체 자체를 꺼내서 쓰는 방식
//const { count, increment } = useCounterStore(); // 구조분해할당으로 꺼내는 방식  -> 구조분해 할당시 값만 복사 -> 반응형이 깨짐

const counter = useCounterPersistStore();
const { count, increment } = useCounterPersistStore();
</script>

<style scoped></style>
